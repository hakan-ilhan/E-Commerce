const initialState = {
  productList: [],
  totalProductcount: null,
  pageCount: null,
  activePage: null,
  fetchState: "",
};
const productReducer = (state = initialState, action) => {
  switch (action.type) {
  }
  return state;
};

export default productReducer;
